services:
  - type: web
    name: nepse-proxy
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: GLM_API_KEY
        value: REMOVED_KEY
