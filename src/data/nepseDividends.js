/**
 * NEPSE Dividend Database — EMPTIED
 * All dividend, bonus share, and right share data is fetched
 * live via backend endpoint: GET /api/dividend-history/:symbol
 */
export const VERIFIED_DIVIDEND_DATABASE = {};
